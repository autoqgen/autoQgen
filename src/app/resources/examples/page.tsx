import type { Metadata } from "next"; import { ArrowRight } from "lucide-react"; import Link from "next/link"; import { PublicShell } from "@/components/marketing/PublicPage"; import { Container, Eyebrow } from "@/components/marketing/shared";
export const metadata: Metadata = { title: "Examples", description: "Explore static examples of the questions and papers you can organise with AutoQgen." };
const examples = [{ subject: "Biology", topic: "Cell structure", type: "Multiple choice", difficulty: "Medium", question: "Which structure controls the movement of substances into and out of a cell?", answer: "Cell membrane" }, { subject: "Mathematics", topic: "Fractions", type: "Short answer", difficulty: "Foundational", question: "A recipe uses 3/4 cup of flour. How much flour is needed for half the recipe?", answer: "3/8 cup" }, { subject: "Physics", topic: "Newton's laws", type: "Structured", difficulty: "Challenging", question: "Explain how the net force on an object affects its acceleration.", answer: "Acceleration is proportional to net force and inversely proportional to mass." }];
export default function ExamplesPage() { return <PublicShell><main id="main"><section className="border-b border-slate-200 bg-slate-50/70 py-20"><Container><Eyebrow>Examples</Eyebrow><h1 className="mt-4 text-4xl font-semibold tracking-tight text-slate-900 sm:text-5xl">See the shape of a reusable question</h1><p className="mt-5 max-w-2xl text-lg leading-relaxed text-slate-600">These are static examples to show the context AutoQgen keeps alongside question content. They are not dynamically generated on this page.</p></Container></section><section className="py-16"><Container className="flex flex-col gap-5">{examples.map((example) => <article key={example.question} className="rounded-2xl border border-slate-200 bg-card p-6"><div className="flex flex-wrap gap-2 text-xs font-semibold text-brand-700"><span>{example.subject}</span><span>/</span><span>{example.topic}</span><span>/</span><span>{example.type}</span><span>/</span><span>{example.difficulty}</span></div><h2 className="mt-4 text-lg font-semibold text-slate-900">{example.question}</h2><p className="mt-3 text-sm text-slate-600"><strong className="text-slate-800">Example answer:</strong> {example.answer}</p></article>)}</Container></section><section className="border-t border-slate-200 py-12"><Container><Link href="/register" className="inline-flex items-center gap-2 rounded-lg bg-brand-600 px-5 py-2.5 text-sm font-semibold text-white hover:bg-brand-700">Create your own bank <ArrowRight className="h-4 w-4" /></Link></Container></section></main></PublicShell>; }