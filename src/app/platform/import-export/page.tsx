import type { Metadata } from "next";
import { PublicPage, PublicShell } from "@/components/marketing/PublicPage";
export const metadata: Metadata = { title: "Import & Export", description: "Bring existing questions into AutoQgen and export finished papers as PDF or DOCX." };
export default function ImportExportPage() { return <PublicShell><PublicPage eyebrow="Platform · Import & export" title="Bring your existing work forward" description="Bulk import existing questions with row-level validation, then export the papers you build in the formats AutoQgen supports: PDF and DOCX." kind="import" benefits={["Import up to 500 questions at once with per-row validation.", "Catch invalid rows and duplicates before they reach your bank.", "Deliver finished papers as PDF or DOCX for the next step in your process."]} steps={["Prepare your question import.", "Upload and inspect row-level validation.", "Resolve issues and add valid questions.", "Build and export a paper."]} /></PublicShell>; }