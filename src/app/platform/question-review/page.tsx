import type { Metadata } from "next";
import { PublicPage, PublicShell } from "@/components/marketing/PublicPage";
export const metadata: Metadata = { title: "Question Review", description: "Review generated and submitted questions before using them in AutoQgen papers." };
export default function ReviewPage() { return <PublicShell><PublicPage eyebrow="Platform · Review" title="Put a thoughtful review between draft and paper" description="The review queue makes generated and submitted questions visible to the people responsible for quality. Edit, approve, reject, and keep the bank dependable." kind="review" benefits={["See pending work in one focused queue.", "Use draft, pending, approved, and rejected states to keep quality visible.", "Prevent unchecked content from quietly entering a final paper."]} steps={["A generated or submitted question enters review.", "Open it and check the wording, answer, and context.", "Edit or reject when it needs work.", "Approve it when it is ready for reuse."]} /></PublicShell>; }